# Journal — LOGI-0005 (Driver CRUD)

Append-only. Newest entries at the bottom. Sections per arm.

## architect-arm (2026-09-20 by main-orchestrator)
- **What:** Spec-first phase per `03-spec-driven-workflow.md` (PRD F3). Authored
  `specs/features/LOGI-0005-drivers-crud.md` (AC-1..AC-9: create happy path, fullName
  required, licenseNumber unique → 409 on POST and PUT, status enum
  Active/OffDuty/Suspended default Active, optional user link — nonexistent userId → 400,
  user already linked to another driver → 409 (User 1---1 Driver, schema entity overview),
  PUT with userId null clears the link, pagination + q/status filters, get/update/delete +
  404s, RBAC 401/403 from day one per ADR-007 — Driver role 403 on master-data reads) and
  extended `contracts/v1-openapi.yaml` additively (`DriverRequest`/`DriverResponse`,
  `/drivers` + `/drivers/{id}`; x-roles mirror the vehicle matrix: read
  [Admin, Dispatcher, Viewer], write [Admin, Dispatcher], delete [Admin]).
- **Gates:** spectral (`--ruleset contracts/.spectral.yaml`) → **0 errors**;
  `git diff --numstat contracts/` → **149+/0−** (purely additive, no existing path/schema
  touched); 5 new operationIds (listDrivers/createDriver/getDriver/updateDriver/
  deleteDriver), 20 total, no dupes.
- **Migration plan (for the database arm):** `drivers` is already approved in
  `04-database-schema.md` §drivers — additive migration `<Timestamp>_LOGI-0005_AddDrivers`
  (id / user_id FK→users.id NULL / full_name / license_number UNIQUE / phone / status;
  unique index on `license_number`; FK delete behaviour default NO ACTION). No new ADR
  (enums Active/OffDuty/Suspended pre-approved; no created_at column — the response
  intentionally has none, list orders by id asc).
- **Human checkpoint:** SPEC_REVIEW→SPEC_APPROVED and CONTRACT_REVIEW→CONTRACT_APPROVED
  approved 2026-09-20 as presented; spec front matter flipped to `spec_approved`.
- **Deferred (not decided here):** whether the linked user must hold role=Driver (v1
  validates existence + 1:1 only — flagged in spec §7); delete-when-referenced-by-route
  409 enforcement lands with the `routes.driver_id` FK in LOGI-0009; driver status
  lifecycle is free-form PUT (no BRD rule — would need a BRD revision).
- **What the next agent needs:** GitHub CI run #10 (`fa324be`, the LOGI-0004 push) verified
  green before this arm. Backend: MediatR CRUD mirroring vehicles + unique-license 409 +
  userId existence (400) / 1:1 (409) checks + nullable link. Frontend: regenerate typed
  client + `features/drivers/*` (no createdAt in the UI model). QA:
  `tests/e2e/drivers.spec.ts` for AC-1..AC-9.

## backend-arm (2026-09-21 by orchestrator-inline — main-thread inline execution, user-approved override of the fresh-window rule)
- **What:** Backend implementation per `state/plans/LOGI-0005-backend.plan.md` (steps 1-5, every
  gate green, per-step commits 2195d47 → 80a4664 → da6df72 → 204708e). `Driver` entity
  (FullName/LicenseNumber/Phone?/Status/long? UserId; Create/Update trim inputs; **no createdAt**
  — schema §drivers defines none), `IAppDbContext.Drivers`, EF mapping (drivers table snake_case;
  **unique index** `ix_drivers_license_number`; nullable FK `user_id` → `users.id`, constraint
  `fk_drivers_users`, delete **NO ACTION**), DriverCommands/DriverQueries (MediatR, mirroring
  vehicles), `/api/v1/drivers` endpoints with the contract's x-roles matrix wired in Program.cs,
  and `DriverEndpointsTests.cs` (11 tests, AC-1..AC-9).
- **Gates:** `dotnet build src/backend/LogiFlow.sln` pass (steps 1-3); `dotnet test
  src/backend/LogiFlow.Api.Tests` **42/42 passed** (auth + warehouses + vehicles + drivers).
- **Migration:** `20260921041528_LOGI-0005_AddDrivers` (+ .Designer.cs) generated with dotnet-ef
  9.0.8 (`--project LogiFlow.Infrastructure --output-dir Persistence/Migrations`); snapshot
  updated. The generated FK emits **no ON DELETE clause** = SQLite default NO ACTION (matches the
  approved schema).
- **User-link semantics (as locked in the plan):** nonexistent userId → **400** with
  `errors.userId` (handler-thrown FluentValidation `ValidationException` keyed `UserId`, camel-
  cased by ExceptionHandlingMiddleware); userId already linked to another driver → **409**
  `ConflictException` on POST and PUT (checks exclude self); PUT with null **or omitted** userId
  clears the link (the nullable DTO collapses both — the frontend must always send the current
  link value on edit). Existence (400) is checked before the 1:1 rule (409).
- **1:1 race note:** the link is a handler pre-check only — the approved schema has no unique
  index on user_id (deliberately not added without a checkpoint), so a concurrent-create race
  could double-link. Accepted for v1 (SQLite dev, single-threaded tests).
- **Convention artifact (checkpoint record):** EF Core auto-added a **non-unique**
  `IX_drivers_user_id` backing index for the FK — standard EF convention for every FK, not the
  forbidden *unique* user_id index; the at-most-one-driver-per-user rule stays handler-enforced.
- **Test notes:** seeded user ids are resolved from the login response's `user.id` (the spec's
  "user id 7" is illustrative); the Driver-role account is asserted **403 even on reads** (master
  data is not a driver-persona surface, spec §2); Viewer POST 403 leaves the row count unchanged;
  anonymous 401 is asserted on all five operations.
- **Deferred (unchanged from architect arm):** role=Driver requirement on the link;
  delete-when-referenced-by-route 409 (lands with `routes.driver_id` in LOGI-0009); driver status
  lifecycle (free-form PUT in v1; would need a BRD revision).
- **What the next agent needs (frontend):** regenerate the typed client from
  `contracts/v1-openapi.yaml` (DriverRequest/DriverResponse are already in the contract); build
  `src/frontend/src/features/drivers/*`; the UI model carries **no createdAt**; lists are ordered
  id asc; PUT must always send the current `userId` (omitted ⇒ clears the link). QA afterwards:
  `tests/e2e/drivers.spec.ts` AC-1..AC-9.

## frontend-arm (2026-09-21 by orchestrator-inline — main-thread inline execution, user-approved override of the fresh-window rule)
- **Recovery note (read this first):** the arm's first, interrupted inline run left production code in
  the working tree with **no locked plan, no tracker events, no commits and no micro-log**
  (`resume-check` verdict `mid_step`). The plan `state/plans/LOGI-0005-frontend.plan.md` was therefore
  written as an *audit* plan: steps 2-3 re-verify that work against the contract/spec **by gate**, not
  by trust. `DriverFormDialog.tsx` had been written to a stray `C/Sudipto/...` path (bad redirect) and
  was relocated; the stray `C/` tree and a root `nul` artifact were deleted.
- **What:** typed client regenerated (`Driver`/`DriverInput`/`DriverStatus` + 5 CRUD methods),
  `viewDrivers`/`editDrivers`/`deleteDrivers` capabilities, MSW `driversDb` +
  `seedDriver`/`resetDriversDb` + `/drivers` handlers (own role matrix: Driver **403 even on reads**,
  licence 409, userId 400-then-409, 404s), `features/drivers/{schema,hooks,DriversPage,DriverFormDialog}`,
  the Drivers tab gated on `viewDrivers`, and `DriversPage.test.tsx` (16 specs).
- **Gates:** `npm run generate:api` → **zero diff** on `api/schema.d.ts` (238 insertions unchanged, so
  the client is provably generated from the contract); `npx tsc --noEmit` clean; `npm test` **41/41**
  across 4 suites (warehouses 8, auth 10, vehicles 7, drivers 16); `npm run build` (tsc + vite) OK.
- **Corrections made during the audit (all inside the §2 manifest):** (1) `DriversPage.handleFormSubmit`
  took a loose inline object whose `status` was optional while the generated `DriverRequest` requires it
  — TS2345/TS2322; now typed `DriverInput` like the vehicles page; (2) `TablePagination` sat *inside*
  `<Table>` (React `validateDOMNesting`) — moved to a `TableContainer` sibling; (3) the search field used
  `InputProps`, so its aria-label never reached the input — switched to `inputProps`.
- **MUI gotcha (recorded for future arms):** `Tabs` in @mui/material 5.x injects
  `value`/`onChange`/`indicator` into its children with `cloneElement` and has **no TabsContext**, so a
  `Tab` wrapped in another component renders but never switches tabs (the first attempt failed 15/16
  specs). Tab state now lives in `MasterDataTabs`, a component *below* `AuthProvider`, with the `Tab`
  elements as direct children of `Tabs`.
- **Findings deferred (not fixed here):** pre-existing `validateDOMNesting` warning in
  `WarehousesPage.tsx` line 187 (LOGI-0001) — out of this arm's manifest, candidate follow-up ticket; the
  stale detached worktree `.kilo/worktrees/lime-spirit` (2433dc8) is still on disk; the tab gate is UX
  affordance only (server + mocks return 403 regardless).
- **What the next agent needs (qa):** `tests/e2e/drivers.spec.ts` for AC-1..AC-9 against the real API.
  Contract x-roles: read [Admin, Dispatcher, Viewer], write [Admin, Dispatcher], delete [Admin]; the
  Driver role gets 403 on **reads** too. Duplicate licence → 409 on POST **and** PUT; nonexistent userId →
  400 with `errors.userId`; a user already linked → 409 (self excluded); PUT with null/omitted `userId`
  clears the link. No `createdAt` on the resource; list ordered id asc; in the UI the Drivers tab is
  hidden for the Driver role.
