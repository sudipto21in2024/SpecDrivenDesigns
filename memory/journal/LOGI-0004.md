# Journal — LOGI-0004 (Vehicle CRUD)

Append-only. Newest entries at the bottom. Sections per arm.

## architect-arm (2026-09-20 by main-orchestrator)
- **What:** Spec-first phase per `03-spec-driven-workflow.md` (PRD F2). Authored
  `specs/features/LOGI-0004-vehicles-crud.md` (AC-1..AC-9: happy path, plate
  required+unique→409, type enum, capacity>0, status enum default Available,
  pagination+status/type filters, get/update/delete+404s, RBAC 401/403 from day one
  per ADR-007) and extended `contracts/v1-openapi.yaml` additively
  (`VehicleRequest`/`VehicleResponse`, `/vehicles` + `/vehicles/{id}`, `Conflict`
  response component; x-roles mirror the warehouse matrix: read [Admin, Dispatcher,
  Viewer], write [Admin, Dispatcher], delete [Admin]).
- **Gates:** spectral (`--ruleset contracts/.spectral.yaml`) → **0 errors**;
  `git diff contracts/` → **143+/0−** (purely additive, no existing path/schema touched);
  15 operationIds, no dupes.
- **Migration plan (for the database arm):** `vehicles` table is already approved in
  `04-database-schema.md` §vehicles — additive migration
  `<Timestamp>_LOGI-0004_AddVehicles` (columns id/plate_number UNIQUE/type/capacity_kg/
  status/created_at, unique index on `plate_number`). No new ADR (enums
  Van/Truck/Trailer + Available/InRoute/Maintenance pre-approved).
- **Deferred (not decided here):** delete-when-referenced-by-route (409 declared,
  enforcement lands with the `routes.vehicle_id` FK in LOGI-0009); vehicle status
  lifecycle (free-form PUT in v1 — no BRD rule; a lifecycle would need a BRD revision).
- **What the next agent needs:** this arm holds spec+contract only — backend, frontend
  and QA arms fan out after the human checkpoint (SPEC_REVIEW→SPEC_APPROVED,
  CONTRACT_REVIEW→CONTRACT_APPROVED). Backend: MediatR CRUD mirroring warehouses +
  unique-plate 409; frontend: regenerate typed client + `features/vehicles/*`;
  QA: `tests/e2e/vehicles.spec.ts` for AC-1..AC-9.

## backend-arm (2026-09-20 by main-orchestrator)
- **What:** MediatR CRUD mirroring warehouses. `Vehicle` entity (`Create`/`Update`,
  plate trimmed; type/status TEXT — closed sets enforced by FluentValidation);
  `ConflictException` → 409 `https://logiflow.dev/errors/conflict`;
  `VehicleCommands.cs` (duplicate-plate pre-check + UNIQUE-constraint race backstop →
  409 on POST and PUT; status omitted → Available); `VehicleQueries.cs` (paged list,
  q = plate contains case-insensitive, status/type exact filters, 400 on bad enum
  value); endpoints RBAC = contract x-roles (read Admin/Dispatcher/Viewer, write
  Admin/Dispatcher, delete Admin-only); migration
  `20260920060044_LOGI-0004_AddVehicles` (`dotnet ef` 9.0.8 was available —
  no hand-authoring needed) + unique `IX_vehicles_plate_number` index.
- **Design notes:** plate uniqueness is SQLite BINARY (case-sensitive after trim) —
  spec silent, v1 exact match; delete-referenced-by-route 409 stays declared-only
  until LOGI-0009 creates the `routes` FK (v1 hard-deletes).
- **Gates:** `dotnet build` 0 warnings/0 errors; `dotnet test` **30/30 green**
  (20 pre-existing warehouse+auth + 10 new `VehicleEndpointsTests` AC-1..AC-9).

## frontend-arm (2026-09-20 by main-orchestrator)
- **What:** typed-client regen + `features/vehicles/*`. `npm run generate:api` (openapi-typescript 7.13) regenerated `schema.d.ts` — Vehicle* diff additive only. `client.ts` gained `Vehicle`/`VehicleInput` types + `listVehicles`/`getVehicle`/`createVehicle`/`updateVehicle`/`deleteVehicle` (list takes page/pageSize/q/status/type). `permissions.ts` gained `viewVehicles`/`editVehicles`/`deleteVehicles` (matrix matches warehouses: read all roles, write Admin/Dispatcher, delete Admin-only). `mocks/handlers.ts` gained `vehiclesDb`/`seedVehicle`/`resetVehiclesDb` + full `/api/v1/vehicles` MSW handlers (paged q/status/type, validate→400, duplicate plate→409 Conflict, 404s, same authorize matrix). `renderApp.tsx` `resetMocks` clears vehicles. `features/vehicles/schema.ts` (Zod plate 1..20, type/status enums, capacity > 0 string-coerced); `hooks.ts` (query keys + invalidation idiom from warehouses); `VehicleFormDialog.tsx` (enum selects, status defaults Available, 409 → plateNumber field error, other 400 errors → field mapping, unexpected → Alert); `VehiclesPage.tsx` (search + status/type filter selects, MUI paged table with totalPages, role-gated New/Edit/Delete — controls hidden on the client but the server 403s regardless); `App.tsx` nav Tabs Warehouses/Vehicles (single QueryClient, local tab state — no router, matches existing chrome).
- **Test fix:** the vehicles suite initially failed `tab-vehicles` not-found (session not restored before Tabs render); fixed per the warehouse-suite precedent — `beforeEach(resetMocks)` + `renderAppAs(role)` + `await user.click(await screen.findByTestId('tab-vehicles'))`.
- **Gates:** `npx tsc --noEmit` clean; `npm test` → **3 suites 25 tests green** (warehouses 8, auth, vehicles 7); duplicate plate surfaces as a plate field error in the UI, no silent failure.

