# Journal — LOGI-0003 (Auth & Roles)

Append-only. Newest entries at the bottom. Sections per arm.

## backend-arm (reconciled 2026-09-19 by main-orchestrator)
- **What:** Auth stack (Identity + JWT issuance, refresh rotation, ProblemDetails writer,
  RBAC role enforcement on warehouse endpoints, seeded users) was already implemented
  **before the platform existed** — uncommitted in the working tree. This session
  reconciled the stale draft plan to the actual tree (§2 manifest now matches; the old
  draft's `RoleAuthMiddleware.cs`/`AuthTests.cs` approach was superseded by
  `Authorization/CurrentUser.cs` + `AuthEndpointsTests.cs`), validated + locked
  `state/plans/LOGI-0003-backend.plan.md`, claimed the arm, and verified gates.
- **Files:** see `state/plans/LOGI-0003-backend.plan.md` §2 (whole `src/backend` auth stack).
- **Gates:** `dotnet test` **20/20 green**; frontend `npm test` **18/18 green** (vitest, 2026-09-19).
  OpenAPI spectral lint pending (runs in CI; not executed locally yet).
- **Known deviations (documented in plan §5):** uncommitted out-of-arm changes exist —
  frontend `features/auth/*` + `api/tokenStore.ts` + `test/renderApp.tsx` (→ `LOGI-0003-frontend`),
  `contracts/v1-openapi.yaml` + ADR-007 + schema doc (architect/docs artifacts per spec §9),
  `tests/e2e/auth.spec.ts` + `login.page.ts` + `support/` (→ `LOGI-0003-qa`).
  `warehouses.spec.ts` still carries the in-flight `seedWarehouse` signature change.
  Stray runtime artifacts `src/backend/LogiFlow.Api/e2e-logiflow.db-shm/-wal` — never commit;
  qa arm should add `*.db-shm`/`*.db-wal` to `.gitignore`.
- **What the next agent needs:** role names in claims come from `LogiFlow.Domain/Security/Roles.cs`
  enum; e2e-qa must authenticate via `POST /api/v1/auth/login` (seeded creds printed in
  `SeedData.cs`) before hitting protected routes. Frontend arm plans need `LOGI-0003-backend`
  plan as dependency (status: locked).

## frontend-arm (reconciled 2026-09-19 by main-orchestrator)
- **What:** SPA auth per AC-12 — `tokenStore.ts` (ADR-007 localStorage strategy, in-memory
  fallback), `AuthContext`/`AuthGate`/`LoginPage`, `permissions.ts` capability map mirroring
  contract `x-roles`, refresh-once-retry `client.ts`, role-gated `WarehousesPage`, msw auth
  handlers, `renderApp.tsx` test harness, contract-regenerated `schema.d.ts`. All pre-existing
  uncommitted; reconciled to locked plan `state/plans/LOGI-0003-frontend.plan.md`
  (depends on backend plan, done).
- **Files:** see plan §2 (whole `src/frontend` auth surface).
- **Gates:** `npm test` **18/18** (10 auth + 8 warehouse) and `npm run build`
  (tsc + vite) **green**, 2026-09-19. Commit `2c254a7`.
- **Platform note:** fresh plan files written by the editor land with CRLF, which breaks
  `plans.mjs` front-matter regex (`^---\n`) — strip with `sed -i 's/\r$//'` before
  `validate-plan`/`plan lock`.
- **What the next agent needs (qa):** UI flows: no token → login screen; Viewer read-only;
  Dispatcher no Delete; sign-out clears tokens. E2E should drive the real login page against
  the seeded users (`SeedData.cs` creds) — the frontend dev server proxies `/api` to the API.
  Remaining uncommitted: e2e files (`auth.spec.ts`, `login.page.ts`, `support/`,
  `global-setup.ts`, `warehouses.page.ts`, `warehouses.spec.ts` with in-flight
  `seedWarehouse` signature change), plus docs/architect artifacts (ADR-007, openapi
  contract, schema doc, both specs, `Docs/business-rules/`).

## qa-arm (sealed 2026-09-19 by main-orchestrator)
- **What:** Established the Playwright e2e gate. Locked plan `state/plans/LOGI-0003-qa.plan.md`
  (deps backend+frontend, both done); reconciled the pre-existing uncommitted QA tree:
  `seedWarehouse` signature was already aligned in-tree (`request, accessToken, name, address?`,
  all 3 call sites match) — verified, not rewritten. Page objects: `login.page.ts`
  (gotoAnonymous clears localStorage pre-boot), `warehouses.page.ts` (auth-aware `goto(role)`,
  `signOut`, `findRow` = server-side search + wait), `global-setup.ts` reset via API as seeded
  Admin. Shared helpers in `support/api.ts` (SEED_USERS/SEED_PASSWORD mirror `SeedData`).
- **Two root causes fixed from the prior failed run (3 failures → 0):**
  1. Pagination — the table pages 5 rows and the shared DB accumulates rows, so seeded rows land
     beyond page 1 and `row(name).toBeVisible()` never sees them: all row-visibility asserts now
     go through `findRow()` (auth.spec AC-12 Viewer/Dispatcher/Admin; warehouses.spec AC-6/AC-7
     were already fixed in-tree).
  2. AC-6 rename — after the edit the list is still server-side-filtered by the OLD name, so the
     renamed row can never appear: the test re-searches by the new name, then asserts old hidden.
- **Gates:** `npx playwright test` **21/21 green** (run 2, 20.2s, 2 workers; run 1 was 20/21 —
  the AC-6 stale-filter, journaled + micro-logged before the fix). `tsc --noEmit` over the e2e
  tree green (frontend workspace tsc 5.9.3, `--typeRoots ../../src/frontend/node_modules/@types`).
  Commits: `87d5d57` (step 1), `95ea2f6` (step 2), `ec796bb` (step 3), `ce485a8` (step 4).
- **Decisions:** kept `fullyParallel: false` + default 2 workers (no config change) — run 2 proved
  the spec-file interleave harmless once asserts are search-driven (name-token scoped, no count
  asserts). CI note: strategy doc wants per-worker SQLite isolation; the single shared throwaway
  file remains safe for this suite.
- **Platform housekeeping:** `.gitignore` += `*.db-shm`/`*.db-wal`; deleted previously TRACKED
  runtime sidecars `src/backend/LogiFlow.Api/logiflow.db-shm/-wal`; cleared stale pre-fix
  `test-results/` traces. Gotcha: `git check-ignore` consults the index by default — tracked
  files report "not ignored" even when a pattern matches.
- **What the next agent needs:** LOGI-0003 is fully green — backend `dotnet test` 20/20,
  frontend vitest 18/18 + build, e2e 21/21 (spec AC-1..12 all mapped to ≥1 passing test).
  The remaining uncommitted tree is docs/architect only: `Docs/adr/007-auth-model-identity-jwt-rbac.md`,
  `contracts/v1-openapi.yaml`, `Docs/ProjectTechGuidence/04-database-schema.md`,
  `specs/features/LOGI-0003-auth-roles.md`, `specs/features/LOGI-0002-sla-business-rules.md`,
  `Docs/business-rules/` (ownership to be confirmed against LOGI-0002). Commit via a docs arm or
  fold into LOGI-0002's plan; `state/tasks.json`+`state/events.jsonl` land with the platform chore.

### Successor block — docs arm / LOGI-0002
- Ticket: LOGI-0003 CLOSED (all three arms done). Next: LOGI-0002 (SLA & business rules,
  spec-only doc) per the original plan order, or a docs arm for the architect artifacts first.
- State: `master` @ `ce485a8`; qa arm sealed; handoff qa→docs recorded in tracker.
- Read first: `memory/active.md` → `specs/features/LOGI-0002-sla-business-rules.md` →
  `Docs/business-rules/` (confirm it belongs to LOGI-0002 before committing) →
  `Docs/adr/007-auth-model-identity-jwt-rbac.md` (content review) → `contracts/v1-openapi.yaml`
  (uncommitted; referenced by regenerated `schema.d.ts` + backend auth code).
- Plan ptr: `state/plans/LOGI-0003-qa.plan.md` (status done, 4/4 steps ticked).
- Gates at seal: playwright 21/21 · tsc (e2e) green · git tree free of runtime artifacts.

## docs-arm (sealed 2026-09-20 by main-orchestrator)
- **What:** Committed the four LOGI-0003 architect/docs artifacts that the prior arms implemented
  against, after full content review: `Docs/adr/007-auth-model-identity-jwt-rbac.md` (Accepted;
  decisions #1–#6 match the shipped stack), `contracts/v1-openapi.yaml` (root `security:
  [BearerAuth]`; `security: []` ONLY on /health + /auth/login|refresh|logout; 401/403 declared on
  every protected operation; 404→`NotFound` component; camelCase `TokenResponse`;
  `/auth/logout` 204 idempotent + `/auth/me`; `/health` drops `x-roles` — anonymous by design for
  CI/Playwright probes), `Docs/ProjectTechGuidence/04-database-schema.md` (Identity-extended
  `users` + hashed `refresh_tokens` + indexing rules), `specs/features/LOGI-0003-auth-roles.md`
  (front matter → `done`, mirroring LOGI-0001 convention).
- **Gate:** content review vs implemented code — contract ⇔ 21/21 e2e, schema ⇔ shipped migration;
  commit `d1ec277` is manifest-exact (4 files, 416+/41−; zero `src/**` or `tests/**` changes).
  Spectral lint stays CI-only (never run locally, per journal precedent).
- **Open question resolved:** `Docs/business-rules/BR-sla-rules.md` header states
  `Ticket: LOGI-0002` → its commit belongs to the LOGI-0002 docs arm, NOT this one.
- **Housekeeping:** removed stray untracked file `nul` (created by a POSIX-shell `2>nul`
  redirection in an earlier session).
- **What the next agent needs (LOGI-0002 docs arm):** commit
  `specs/features/LOGI-0002-sla-business-rules.md` + `Docs/business-rules/BR-sla-rules.md`
  (spec-only ticket; its front matter is already `status: done`); verify AC-1..AC-7 against
  `11-BRD.md` §6 (lines 62–67) before committing; refresh `Docs/PROJECT_STATUS.md`
  (rows LOGI-0002/0003 + §7 item 9) in the same arm. `state/*` snapshots land with the platform
  chore commit.
- **Addendum (post-seal, 2026-09-20):** spectral lint run locally for the first time —
  `npx -y @stoplight/spectral-cli lint contracts/v1-openapi.yaml` → **0 errors** ("No results
  with a severity of 'error' found"). The "spectral is CI-only" caveat from earlier arms is
  closed; commits through `55c4641` pushed to `origin/master`.
