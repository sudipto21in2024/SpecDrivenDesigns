# Journal — LOGI-0003 (Auth & Roles)

Append-only. Newest entries at the bottom. Sections per arm.

## backend-arm (reconciled 2026-09-19 by main-orchestrator)
- **What:** Auth stack (Identity + JWT issuance, refresh rotation, ProblemDetails writer,
  RBAC role enforcement on warehouse endpoints, seeded users) was already implemented
  **before the platform existed** — uncommitted in the working tree. This session
  reconciled the stale draft plan to the actual tree (§2 manifest now matches; the old
  draft's `RoleAuthMiddleware.cs`/`AuthTests.cs` approach was superseded by
  `Authorization/CurrentUser.cs` + `AuthEndpointsTests.cs`), validated + locked
  `state/plans/LOGI-0003-backend.plan.md`, claimed the arm, and verified gates.
- **Files:** see `state/plans/LOGI-0003-backend.plan.md` §2 (whole `src/backend` auth stack).
- **Gates:** `dotnet test` **20/20 green**; frontend `npm test` **18/18 green** (vitest, 2026-09-19).
  OpenAPI spectral lint pending (runs in CI; not executed locally yet).
- **Known deviations (documented in plan §5):** uncommitted out-of-arm changes exist —
  frontend `features/auth/*` + `api/tokenStore.ts` + `test/renderApp.tsx` (→ `LOGI-0003-frontend`),
  `contracts/v1-openapi.yaml` + ADR-007 + schema doc (architect/docs artifacts per spec §9),
  `tests/e2e/auth.spec.ts` + `login.page.ts` + `support/` (→ `LOGI-0003-qa`).
  `warehouses.spec.ts` still carries the in-flight `seedWarehouse` signature change.
  Stray runtime artifacts `src/backend/LogiFlow.Api/e2e-logiflow.db-shm/-wal` — never commit;
  qa arm should add `*.db-shm`/`*.db-wal` to `.gitignore`.
- **What the next agent needs:** role names in claims come from `LogiFlow.Domain/Security/Roles.cs`
  enum; e2e-qa must authenticate via `POST /api/v1/auth/login` (seeded creds printed in
  `SeedData.cs`) before hitting protected routes. Frontend arm plans need `LOGI-0003-backend`
  plan as dependency (status: locked).
