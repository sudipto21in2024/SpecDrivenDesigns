# Journal — LOGI-0003 (Auth & Roles)

Append-only. Newest entries at the bottom. Sections per arm.

## backend-arm (example entry — replace with real content when the arm runs)
- **What:** JWT issuance + role middleware per AC-1..AC-4.
- **Files:** see `state/plans/LOGI-0003-backend.plan.md` §2.
- **Gates:** `dotnet test` green, spectral 0 errors.
- **What the next agent needs:** role names in claims use `LogiFlow.Domain` enum; e2e-qa must
  seed an Admin user via the auth endpoints before testing protected routes.
