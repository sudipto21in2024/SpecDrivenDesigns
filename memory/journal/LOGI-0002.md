# Journal — LOGI-0002 (SLA business-rules reference, spec-only)

Append-only. Newest entries at the bottom. Sections per arm.

## docs-arm (sealed 2026-09-20 by main-orchestrator)
- **What:** Spec-only ticket completed. Verified spec AC-1..AC-7 against `11-BRD.md` §6
  (lines 62–67) and the BR doc, then committed `Docs/business-rules/BR-sla-rules.md`
  (authoritative BR-1/BR-2 reference) + `specs/features/LOGI-0002-sla-business-rules.md`
  (front matter already `status: done`) — commit `896d3ab` (2 files, 284 insertions,
  manifest-exact). One gap closed during review: AC-4 whole-second precision is now explicit
  (§3 of the BR doc) — all SLA instants are whole-second ISO8601 UTC, `now` truncated before
  comparison.
- **AC verdicts:** AC-1 authority + no-restate PASS · AC-2 formula 48h/12h anchored to
  server-assigned `created_at` PASS · AC-3 inclusive boundary, exactly {Delivered, Cancelled},
  read-time evaluation PASS · AC-4 gap→closed · AC-5 examples A/B/C + full boundary table PASS ·
  AC-6 enforcement seams (LOGI-0007 inserts `sla_due_at` via a single SlaPolicy unit; read-time
  projection consumed by LOGI-0007 list + LOGI-0012 dashboard; never persisted) PASS ·
  AC-7 out-of-scope + open-questions-with-defaults PASS.
- **Gates:** `896d3ab` docs commit scope = `Docs/business-rules/**` + `specs/**` only;
  `55c4641` PROJECT_STATUS refresh (rows #2/#3 → DONE, §7 item 9 closed). Playwright DoD item
  deliberately N/A per spec §9 (no runtime behaviour — executable form lands in LOGI-0007 as
  a domain policy + unit tests). Docs-only change: no CI-sensitive artifacts touched.
- **What the next agent needs:** next ticket is **LOGI-0004 (Vehicle CRUD + status enum)** —
  spec-first workflow: author `specs/features/LOGI-0004-*.md` (+ contract extension +
  ADR if needed) before any arm plans. LOGI-0007 must consume BR-1 by referencing
  `Docs/business-rules/BR-sla-rules.md` — never restate the constants. Auth context is now
  live: every new endpoint needs `x-roles` + 401/403 declared in `contracts/v1-openapi.yaml`
  and role policies in `LogiFlow.Api/Authorization/Policies.cs` (ADR-007 pattern).